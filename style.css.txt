* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    min-height: 100vh;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #1a0b2e, #3b164f, #701c45);
    color: white;
    overflow-x: hidden;
}

.container {
    text-align: center;
    padding: 70px 20px;
    max-width: 700px;
    margin: auto;
}

h1 {
    font-size: 42px;
    margin-bottom: 10px;
}

h2 {
    font-size: 28px;
    margin-bottom: 20px;
}

.intro {
    font-size: 18px;
    margin-bottom: 30px;
    opacity: 0.9;
}

button {
    padding: 14px 28px;
    border: none;
    border-radius: 30px;
    background: #ff4f81;
    color: white;
    font-size: 17px;
    cursor: pointer;
    transition: 0.3s;
}

button:hover {
    transform: scale(1.08);
    background: #ff2f68;
}

/* Letter */

#letter {
    display: none;
    margin-top: 40px;
    padding: 35px 25px;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(10px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    animation: appear 1s ease;
}

#letter p {
    font-size: 18px;
    line-height: 1.8;
}

.love {
    margin-top: 20px;
    font-size: 20px !important;
}

/* Photo */

.photo-section {
    margin-top: 50px;
}

.photo-box {
    width: 280px;
    height: 280px;
    margin: auto;
    border-radius: 20px;
    border: 2px dashed rgba(255, 255, 255, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.08);
}

.photo-box span {
    opacity: 0.8;
}

.ending {
    margin-top: 50px;
    font-size: 16px;
    opacity: 0.8;
}

/* Floating Hearts */

.heart {
    position: fixed;
    bottom: -50px;
    font-size: 25px;
    animation: float 7s infinite;
    opacity: 0.7;
}

.heart1 {
    left: 10%;
    animation-delay: 0s;
}

.heart2 {
    left: 30%;
    animation-delay: 2s;
}

.heart3 {
    left: 55%;
    animation-delay: 4s;
}

.heart4 {
    left: 75%;
    animation-delay: 1s;
}

.heart5 {
    left: 90%;
    animation-delay: 3s;
}

@keyframes float {
    0% {
        transform: translateY(0);
        opacity: 0;
    }

    20% {
        opacity: 0.8;
    }

    100% {
        transform: translateY(-110vh);
        opacity: 0;
    }
}

@keyframes appear {
    from {
        opacity: 0;
        transform: translateY(20px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}
.photos {
    display: flex;
    justify-content: center;
    gap: 25px;
    flex-wrap: wrap;
    margin-top: 25px;
}

.photo-card {
    width: 280px;
}

.photo-card img {
    width: 100%;
    height: 280px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
}

.photo-card p {
    margin-top: 12px;
    font-size: 17px;
}